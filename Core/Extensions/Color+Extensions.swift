import SwiftUI

extension Color {
    // Custom color palette to replace default blue
    static let customPrimary = Color(red: 0.2, green: 0.6, blue: 0.4) // A nice teal-green
    static let customSecondary = Color(red: 0.3, green: 0.7, blue: 0.5) // Lighter teal
    static let customAccent = Color(red: 0.4, green: 0.8, blue: 0.6) // Bright teal for accents
    static let customBackground = Color(red: 0.95, green: 0.98, blue: 0.96) // Very light teal background
    
    // Alternative color options if you prefer different colors
    static let customPurple = Color(red: 0.5, green: 0.3, blue: 0.8) // Purple option
    static let customOrange = Color(red: 0.9, green: 0.5, blue: 0.2) // Orange option
    static let customPink = Color(red: 0.8, green: 0.4, blue: 0.6) // Pink option
} 